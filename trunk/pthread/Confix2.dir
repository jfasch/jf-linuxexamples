# -*- python -*-

REQUIRE_SYMBOL('jf.externals.pthread', REQUIRED)

h = [
    'condition.h',
    'joinable_thread.h',
    'mutex.h',
    'queue.h',
    'thread_specific.h',
    ]

cc = [
    'condition.cc',
    'joinable_thread.cc',
    'mutex.cc',
    ]

LIBRARY(members=[H(filename=f, install=['jf', 'unix_tools']) for f in h] + [CXX(filename=f) for f in cc])
        
DIRECTORY(['tests'])
