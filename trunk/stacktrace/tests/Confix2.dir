# -*- python -*-

h = [
    'stackframe_test.h',
    'suite.h',
    ]

cc = [
    'suite.cc',
    ]

LIBRARY(members=[H(filename=f, install=['jflinux', 'stacktrace', 'tests']) for f in h] + [CXX(filename=f) for f in cc])
