# -*- python -*-

h = [
    'stack_history_test.h',
    'suite.h',
    ]

cc = [
    'stack_history_test.cc',
    'suite.cc',
    ]

LIBRARY(members=[H(filename=f, install=['jflinux', 'debug', 'tests']) for f in h] + [CXX(filename=f) for f in cc])
